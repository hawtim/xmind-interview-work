import Vue from 'vue'
import {
  Button,
  Row,
  Col,
  Table,
  TableColumn,
  DatePicker,
  Form,
  FormItem,
  Dialog,
  Input,
  Select,
  Option
} from 'element-ui'

Vue.use(Button)
  .use(Row)
  .use(Col)
  .use(Table)
  .use(TableColumn)
  .use(DatePicker)
  .use(Form)
  .use(FormItem)
  .use(Dialog)
  .use(Input)
  .use(Select)
  .use(Option)
